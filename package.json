{
  "name": "webpropiedades",
  "private": true,
  "scripts": {
    "dev": "sh -c 'npm run dev --prefix backend & backend_pid=$!; trap \"kill $backend_pid 2>/dev/null\" EXIT INT TERM; npm run dev --prefix frontend'",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "admin:create": "npm run admin:create --prefix backend"
  }
}
